<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.palpate">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Palpate">
        <activity
            android:name=".HealthStatsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".OtpActivity"
            android:windowSoftInputMode="adjustPan|stateAlwaysHidden"

            />
        <!--        <activity android:name=".DashboardActivity"/>-->
        <activity
            android:name=".ProfileActivity"
            android:windowSoftInputMode="adjustPan" />

        <activity android:name=".DashboardActivity" />

        <activity android:name=".DrawerActivity" />

        <activity android:name=".AddMemberActivity" />

        <activity android:name=".AddMemberDataActivity" />

        <activity android:name=".EditMemberActivity" />

        <activity android:name=".SearchMemberActivity" />


    </application>

</manifest>